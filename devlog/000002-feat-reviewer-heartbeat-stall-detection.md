# 000002-feat-reviewer-heartbeat-stall-detection.md

**Agent:** Muse Code (Muse Spark) @ repository branch feat/reviewer-heartbeat-stall-detection
**Intent:** Give review subagents a progress heartbeat with stall detection, mirror the rules into the report skill, and narrow loop re-reviews to the lenses that found issues with one full confirmation round at the end.

## Decisions

- 2026-09-25T10:49-0700 Mirror heartbeat into agent-review-report: the report skill fans out the same 8 parallel reviewers plus synthesis, so one silent reviewer stalls its single round the same way; its own "keep reviewer prompt bullets in sync" note also required the mirror once the loop brief gained bullets. Adapted, not copied: no round number in progress file names, "report header" and "uncovered" wording, cleanup in section 8.
- 2026-09-25T10:49-0700 Leave agent-review-pr-comments untouched: it runs gh and git directly with no subagents, so heartbeat and stall rules do not apply.
- 2026-09-25T10:49-0700 Track dirty state by lens, not by pillar: findings carry file:line, not pillar tags, so the rerun unit is the reviewer lens (exact at max, paired at high, halved at medium, degenerate at low where the single lens always reruns).
- 2026-09-25T10:49-0700 Close with one full confirmation round: fixes can move a problem into another pillar, and sitting-out lenses never see the fixes otherwise. Confirmation runs all lenses at the loop's effort; actionable findings there resume narrowing on the new dirty set.
- 2026-09-25T10:49-0700 A round that runs every lens counts as full even when every lens was dirty: avoids a redundant confirmation round after an all-dirty round.
- 2026-09-25T10:49-0700 One-line out-of-scope flag: a subset reviewer that spots a severe hazard owned by a sitting-out lens flags it in one line so that lens rejoins the dirty set next round instead of waiting for confirmation.

## What Changed

- 2026-09-25T10:49-0700 `skills/agent-review-report/SKILL.md`: protocol intro points at the stall rule; added stall paragraph (15 minute silence window, one nudge, 5 minute grace, one replacement); added exact-paths/bounded-search, progress-heartbeat, and round-scope reviewer bullets; section 8 cleans up progress files.
- 2026-09-25T10:49-0700 `skills/agent-review-loop/SKILL.md`: fixed effort restated as depth per pillar plus full-round fan-out with narrowing coverage; step 1 gains lens scoping and a round-scope reviewer bullet; step 2 splits the stop condition (actionable narrows, clean subset confirms, clean full finishes); step 6 gains a scope-next summary line; step 7 carries the dirty set and counts the 10 round cap over subset plus confirmation rounds; "full round" reworded in step 4 and the step-down where it meant a whole round rather than full fan-out.
- 2026-09-25T10:49-0700 `README.md`: loop summary notes scoped re-review plus the closing confirmation round.
- 2026-09-25T10:49-0700 `devlog/plans/000002-01-mirror-heartbeat-to-report.md` and `devlog/plans/000002-02-scoped-rereview-confirmation.md`: plans for the mirror and the refactor.

## Commits

- 6cea4d7: feat: reviewer progress heartbeat and stall detection (prior session; loop skill only)

## Next Steps

- Review the uncommitted diff, commit, push with an explicit refspec, and open a PR.

## Review Log (loop self-review, high effort)

- 2026-09-25T12:55-0700 Round 5 (full, lenses 1/3/4 reported, lens 2 missing after original plus retry1 infra timeouts): classified Actionable. Fixed 5 findings in `skills/agent-review-loop/SKILL.md`: zero-report rounds now break the quiet-round streak; step-down drains pending findings via steps 3-5, resets the streak, and carries must-recheck locations; step 5 gains a bounded gate-failure loop (fix, re-fix-check, re-gate, at most 2 re-verifications); non-vacuity revert proofs gain a revert-restore-byte-verify sequence; both step 3/4 stop reports append the terminal status. Also added the step 2 coverage-gap rule (non-nitpick finding outside every assignment pins its closest lens dirty) with matching dirty-set lines in steps 1/7 and the intro. Fix check (direct) caught and fixed two self-gaps (nitpick qualifier, dirty-set sync). Gate: verify-install.sh 159/159 green, ASCII-only. Filed 4 canonical bullets (P1 Zero Evidence Breaks Quiet Streaks, P1 Coverage Gaps Stay Assigned, P3 Mode Transitions Drain Pending Work, P4 Terminal Status On Every Exit); skipped revert-restore (already codified) and gate-loop discipline (common knowledge). Dirty set for round 6: all lenses (1/3/4 actionable, 2 missing), so round 6 runs full.
- 2026-09-25T15:00-0700 Round 6 (full; lens 1 original stalled with late truncated report so retry1 re-ran pillars 1-2, lens 2 original failed on model idle timeout so retry1 re-ran pillars 3-4, plus synthesis): classified Actionable on 9 reconciled findings. Fixed in `skills/agent-review-loop/SKILL.md` plus `README.md`: step 7 coverage-gap cite corrected to the step 1 merge rule; gap routing verb fixed to "enters the dirty set even when it sat out"; README dirty-lens parenthetical gains the coverage gap member; spelling unified to "coverage gap"; revert proofs gain snapshot-first with restore-from-snapshot resume; restore-unverifiable and gate-exhaustion stops gain full stop protocol (learnings, retain, resume, terminal status, quote, cleanup); gate loop names one full step-4 invocation as the re-verification unit; terminal-status contents deduped to the step 7 canonical list with 4 back-refs; subset zero-report rounds now break the quiet-round streak (self-verified consistency with the round 5 principle). Fix check: single canonical enumeration confirmed, 4 back-refs, ASCII-only. Gate: verify-install.sh 159/159 green. Filed 2 canonical P1 bullets (Membership Verbs Cover Joining, Name The Retry Unit); skipped stop-parity/snapshot/streak instances as covered. Dirty set for round 7: all lenses actionable, so round 7 runs full.
- 2026-09-25T15:20-0700 Round 7 (full, all 4 lenses plus synthesis, no stalls): classified Actionable on 11 reconciled findings. Fixed: heartbeat now requires guidance/pillars/learnings reads in the step list plus a line every 10 minutes regardless of activity (both skills mirrored); pillar stall path gains evaluate-from-replacement plus uncancellable-original disposal (both skills mirrored); out-of-scope flag gains `FLAG | lens | file:line | hazard` format; coverage-gap rule redefined to reporting-lens-keeps (deterministic, no closeness metric) with the now-redundant gap-member clauses removed from step 1, step 7, intro, and README; fix-check paths unify on N and gain `-reverify<V>` invocation stems (plus spawns.log example and step 5 pointer); snapshots move under `<progress_dir>/snapshots/<stem>/` with mkdir -m 700 and delete-on-verified-restore; regression-stop quote goes stem-agnostic and gains empty/missing markers; step-down flattened to a count/break/neither rule list. Fix check caught a dropped case in my own flatten (fully-reported non-qualifying full round now explicitly breaks). Gate: verify-install.sh 159/159 green, mirrors byte-identical on edited sentences, ASCII-only. Filed 2 new bullets (P5 Schema Every Routed Signal, P8 Invocation Discriminators For Re-runs) plus 2 merges (P1 gap bullet to reporting-owner, P3 heartbeat bullet with sub-window cadence); skipped snapshot/disposal/streak-flatten/quote-marker instances as covered. Dirty set for round 8: all lenses actionable, so round 8 runs full (8 of 10 cap).
- 2026-09-25T15:44-0700 Round 8 (full; lenses 2 and 4 clean, lenses 1 and 3 actionable, plus synthesis): classified Actionable on 5 reconciled findings. Fixed: streak neither-bullets now require reports received (exactly-one-way restored for the all-silent case); fix-check stall path reconciles the tree from snapshots before replacement and before each next attempt, spawns no replacement over a live mutator (stops with `fix check abandoned` report instead), ignores late original reports, and reconciles before the third-attempt stop; step 7 confirmation exception regains the nitpicks-only case; report cross-lens flag standardizes on the loop FLAG schema. Fix check verified the full stall matrix (cancel-fail, no-snapshot, replacement-stall, third-attempt) and scenario-traced each branch. Gate: verify-install.sh 159/159 green, FLAG shapes identical, ASCII-only. Filed 1 new bullet (P4 Retries Re-establish The Baseline) plus 2 merges (P1 empty-pin bullet with flat-list empty exclusion, P3 heartbeat bullet with confirmed-death-before-mutating-replacement); skipped restatement-drift and flag-mirror instances as covered. Dirty set for round 9: lenses 1 and 3 only (first narrowed round; 9 of 10 cap).
- 2026-09-25T15:52-0700 Round 9 (subset, lenses 1+3, both NO FINDINGS, synthesis skipped per the all-clean rule): classified Clean after a subset round, so confirmation is owed. No fixes, no fix check, no gate re-run (nothing changed). Learnings filed: none (both narrowed lenses came back clean over the round-8 fixes with no new defect, disagreement, or stall to generalize). Next: round 10 full confirmation (10 of 10 cap).
- 2026-09-25T16:05-0700 Round 10 (full confirmation, all 4 lenses NO FINDINGS, synthesis skipped per the all-clean rule): classified Clean after a full round with every lens reported. Stop condition met. No outstanding nitpicks (all prior nitpicks were fixed in their rounds). Verification gate stands from round 8 (tree unchanged since: 159/159 green). Final pillar sweep: no loop-level lessons (no moved problem found, no new failure mode). Learnings filed: none.

## Issues

- 2026-09-25T10:49-0700 Installed from the worktree with uncommitted changes (`./install.sh --upgrade`): loop and report skills refreshed in all five runtimes from branch content ahead of any commit. Reinstall or upgrade again after the branch merges so installed copies track a committed SHA.
